<!DOCTYPE html>
<html>
<head>
  <title><%= @page_title.blank? ? "System Status" : (@page_title + " - ")   %> | Dropkick Studios</title>
  <title>Uptime</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag "detect_timezone.min"%>
  <%= csrf_meta_tag %>
  <%= yield(:head) %>
</head>
<body>

<div class="group">
  <div id="user_nav">  
    <% if current_user %>
      Hello <%= current_user.name %>.  
    <% else %>
      <%= link_to "Sign In", "/auth/twitter" %>  
    <% end %>
  </div>

  <h1>
    <%= (content = content_for(:title)).blank? ? @page_title : content  %>
  </h1>  
</div>
<% if flash[:error]%>
  <div class="error"><%= flash[:error]%></div>
<% end %>
<% if flash[:notice]%>
  <div class="notice"><%= flash[:error]%></div>
<% end %>


<div id="yield">
  <%= yield %>  
</div>

</body>
</html>
